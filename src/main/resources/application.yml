spring:
  profiles:
    active: embedded
  kafka:
    bootstrap-servers: localhost:29092
logging:
  level:
    org:
      apache:
        kafka: WARN
      springframework:
        kafka: WARN
    io:
      confluent:
        kafka: WARN